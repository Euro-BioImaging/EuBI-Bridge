name: eubi-bridge
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pyqt6
  - pip:
    - "git+https://github.com/Euro-BioImaging/EuBI-Bridge.git"
prefix: null  # Users to specify via -p flag or CONDA_PREFIX
